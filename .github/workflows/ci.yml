name: CI

env: {NODE_VERSION: 18}

on:
  push: {branches: [main, 'feature.*']}
  pull_request:

jobs:

  checkout:
    name: checkout
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3